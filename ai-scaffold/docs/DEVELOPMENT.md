# Development guide

## Prerequisites and setup
TBD: supported tool versions, installation commands, and safe local configuration.

## Commands
Document exact setup, lint, test, and build commands before relying on them.
TBD: commands must be verified in this project; no command is assumed to work.

## Testing
Test observable behavior, failure paths, and important integration boundaries.
Use synthetic test data and keep tests deterministic where possible.

## Git workflow
Use develop for changes and pull requests to enter main; preserve individual commits.
TBD: configure the consuming repository's required checks and protections.

## Troubleshooting
TBD: known failures, safe diagnostics, and recovery steps without sensitive logs.
